# Sheet2Scene — Agents Runtime (v1)

This package defines 3 AI agents (DA, Designer, Tech/3D) and exposes HTTP routes to orchestrate them.
It comes with prompts, tokens, a simple runtime and a tiny dashboard.

## Quick start
```bash
npm i
npm run start
# open http://localhost:8790
```
